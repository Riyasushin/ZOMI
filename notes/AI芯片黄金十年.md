# 探讨 SIMD 和 SIMT 的区别与联系

SIMT是SIMD的一种特化，专注于一条指令多线程执行
SIMD一条指令多个数据被操作，和并行不同
SIMD 要求数据严格对齐且同步执行，而 SIMT 允许线程独立寻址且可异步执行，提高了灵活性

# SIMT 与 CUDA 编程的关系；GPU 在 SIMT 编程中的角色
抽象给程序员的接口和物理实现的差异

GPU的物理结构让SIMT成为可能，详见cuda编程


# SIMD、SIMT 与 DSA 架构的联系

## DSA
具体的特殊硬件架构，NPU/TPU 等专门针对 AI 的特殊硬件架构，应用于大规模数据处理、分布式存储等场景。


# 理解 AI 芯片编程体系尤为重要。

# AI 芯片并行分类包括指令级并行（ILP）、数据级并行（DLP）和任务级并行，分别通过流水线技术、向量处理器或并行处理器、多线程或分布式计算实现。
多个指令的不同阶段依次进行，类似并发？？？
数据分开，矩阵分片,...
任务级并行： 可以同时执行的任务，这就是传统讲的

## 流水执行 PPE：
（Pipeline Execution）是流水执行中的一种处理器架构
将取值-->译码-->执行分别放在未使用流水线和使用流水线中进行指令执行，在未使用流水线时每次 for 循环都要占用独立的时间分别进行取值-->译码-->执行相关操作，当使用流水线时，充分利用空余的时间去同时执行不同的指令操作，提高了指令的并行度。

确实类似并发

## 乱序执行（Out-of-Order Execution，OOE）
因此采用乱序执行，在 CPU 空闲时间执行指令 2，由于指令 4 的执行需要指令 1 在写回结果之后，所以需要把依赖性指令移到独立指令后，在指令 1 完全执行之后再执行指令 4，同时 for 循环由硬件通过指令动态展开。

# 并行处理硬件架构主要分为 SISD、SIMD、MISD 和 MIMD 四种

最后一个就是纯纯凑热闹的

# SIMD 架构通过多个处理单元执行相同指令处理不同数据流，广泛应用于 GPU 等并行处理器

# 串行执行（SISD）、数据并行（SIMD）和多线程执行（MIMD/SPMD）是实现并行编程的三种基本方式，分别适用于不同规模和类型的计算任务。


## SPMD（Single Program Multiple Data）
一种并行计算模型，多线程 SPMD 指的是在 SPMD 模型中使用多个线程来执行并行计算任务。在多线程 SPMD 中，每个线程（Thread i）都执行相同的程序，但处理不同的数据，

GPU 的 SIMT 实际上是具体硬件执行 SIMD 指令，采用并行编程模式使用 SPMD 来控制线程的方式

# SIMD 通过向量化指令实现数据并行

# SIMT 则是基于 SIMD 的硬件架构，通过线程束（Warp）实现更灵活的多线程并行执行。

# NVIDIA CUDA 编程模型基于 SPMD，利用 SIMT 硬件架构执行单指令多线程

CUDA的抽象性、易用性

按照使用 CUDA 的难易程度，可以将 CUDA 的使用用户分为三类，分别是初阶、中阶和高阶用户。

初阶用户：掌握 CUDA 并行编程能力，了解 NV SIMT 硬件基础架构，可以拿到并行指令、流水掩盖、并行计算三部分性能。

中阶用户：进一步运用 CUDA 提供的切块 Tiling、流水 Pipeline 能力，进一步获取更高的性能收益。

高阶用户：深入了解 SIMT 12 s细节，解决线程 bank 冲突、精细化流水掩盖、精细化指令使用、极致的切块 Tiling 策略，从而实现极致性能。

# DSA 架构在编程模型和硬件执行模型上还不够成熟，需要构建开放的软硬件架构和易用的开发环境来吸引开发者，并与 CUDA 生态竞争。

# CUDA 提供了从初级到高级不同层次的易用性，通过简化的编程模型和高效的线程管理机制，使得不同水平的开发者都能在 GPU 上实现高性能计算。

# 通过线程组 Warp 隐藏线程指令流水

SIMD 使用户只需要实现单线程，但是需要在前期充分了解 GPU 的硬件架构。

通过线程组 Warp 隐藏线程指令流水。在 CUDA 编程模型中，每一个线程块（thread block）内部需要有很多并行线程，隐式分成了若干个 Warp，每个 Warp 包含串行交错的访存和计算。GPU 通过 Warp Scheduler 动态交错执行，如果一组 Warp0 流水阻塞就会切到下一个 Warp1，隐式通过 Warp 的并行掩盖指令流水阻塞，因此开发者可以得到较好的性能



# 在分支预测方面

TODO 有点蒙，为什么：
> 但是 SIMT 的控制流仍然存在很多问题，因此不推荐在 CUDA 编程中出现大量的 if/else 语句。通常使用 SIMD 流水线来节省控制逻辑上的面积，例如将 Scalar 线程放在 Warps 里面。当 Warp 内部的线程分支到不同的执行路径时，就会发生分支执行冲突，比如当存在 Path 1 和 Path 2 两个分支路径时，可以使得不同时间执行不同的路径，但是这样会增加时耗。



减少分支和连续访存是软件层面、易用性方面需要关注的优化点

通过动态 Warp 合并，动态分支合并和动态 Warp 分组，使得 GPU 并行计算能力大大提高。

可以通过编译器实现分支预测从而让开发者无感
